apiVersion: tekton.dev/v1
kind: PipelineRun
# konflux build manual retrigger - 2026-08-12 17:56:09
metadata:
  annotations:
    build.appstudio.openshift.io/repo: https://github.com/red-hat-data-services/RHOAI-Build-Config?rev={{revision}}
    build.appstudio.redhat.com/commit_sha: '{{revision}}'
    build.appstudio.redhat.com/target_branch: '{{target_branch}}'
    pipelinesascode.tekton.dev/cancel-in-progress: '{{ cel: headers["X-Github-Event"] == "push" ? "false" : "true" }}'
    pipelinesascode.tekton.dev/max-keep-runs: "3"
    pipelinesascode.tekton.dev/on-cel-expression: |
      (
        event == "push"
        && target_branch == "rhoai-3.6-ea.2"
        && ( "helm/rhai-on-openshift-chart/**".pathChanged() || ".tekton/rhai-on-openshift-chart-v3-6-ea-2-push.yaml".pathChanged() )
      )
      || (
        event == "pull_request"
        && target_branch == "rhoai-3.6-ea.2"
        && ( "helm/rhai-on-openshift-chart/**".pathChanged() || ".tekton/rhai-on-openshift-chart-v3-6-ea-2-push.yaml".pathChanged() )
      )
  labels:
    appstudio.openshift.io/application: rhoai-v3-6-ea-2
    appstudio.openshift.io/component: rhai-on-openshift-chart-v3-6-ea-2
    pipelines.appstudio.openshift.io/type: build
  name: rhai-on-openshift-chart-v3-6-ea-2-on-push
  namespace: rhoai-tenant
spec:
  params:
  - name: git-url
    value: '{{source_url}}'
  - name: revision
    value: '{{revision}}'
  - name: additional-tags
    value:
    - '{{target_branch}}-{{revision}}'
  - name: output-image
    value: 'quay.io/rhoai/rhai-on-openshift-chart:{{ cel: headers["X-Github-Event"] == "push" ? pac.target_branch : "pr-" + pac.pull_request_number + "-" + pac.revision }}'
  - name: image-expires-after
    value: '{{ cel: headers["X-Github-Event"] == "push" ? "" : "5d" }}'
  - name: rhoai-version
    value: "3.6.0-ea.2"
  - name: path-context
    value: "helm/rhai-on-openshift-chart"
  - name: hermetic
    value: false
  - name: build-source-image
    value: true
  pipelineRef:
    resolver: git
    params:
    - name: url
      value: https://github.com/red-hat-data-services/konflux-central.git
    - name: revision
      value: '{{ target_branch }}'
    - name: pathInRepo
      value: pipelines/helm-chart-build.yaml
  taskRunSpecs:
  - pipelineTaskName: build-helm-chart
    stepSpecs:
    - name: package-and-push
      computeResources:
        requests:
          memory: 0.3Gi
        limits:
          memory: 4Gi
  taskRunTemplate:
    serviceAccountName: build-pipeline-rhai-on-openshift-chart-v3-6-ea-2
  workspaces:
  - name: git-auth
    secret:
      secretName: '{{ git_auth_secret }}'
status: {}
